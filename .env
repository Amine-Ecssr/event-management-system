#########################################
# Shared Environment Defaults
#########################################
#
# Runtime-specific configuration now lives in:
#   - .env.development      (committed dev defaults)
#   - .env.production       (committed prod template)
#   - *.local variants      (ignored, host-specific secrets)
#
# This base file intentionally stays empty so that
# environment-specific files can take precedence via the
# enhanced loader (server/config/loadEnv.ts).
#
# Add cross-environment values here only if they should be
# shared by every deployment target.

# Keycloak Configuration (for Docker Compose)
KEYCLOAK_URL=http://keycloak:8080
KEYCLOAK_REALM=ecssr-events
KEYCLOAK_CLIENT_ID=ecssr-events-app
KEYCLOAK_CLIENT_SECRET=zUGl2wSob41YEpHuyO2vvPV5TXPdHmCw
KEYCLOAK_REDIRECT_URI=http://localhost:5001/api/auth/callback
KEYCLOAK_ADMIN=admin
KEYCLOAK_ADMIN_PASSWORD=admin

# Superadmin (Keycloak user)
SUPERADMIN_EMAIL=admin@ecssr.ae
SUPERADMIN_PASSWORD=admin123

