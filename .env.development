# Development Environment Configuration
# Copy this file to .env.development.local if you need machine-specific overrides.

# Database Configuration
POSTGRES_DB=ecssr_events_dev
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
# For Docker: use container name 'db'
# For local development outside Docker: use localhost:5433
DATABASE_URL=postgresql://postgres:postgres@db:5432/ecssr_events_dev
DB_RESET_SCHEMAS=public,keycloak

# Session Configuration
SESSION_SECRET=dev-change-me

# Keycloak Configuration
# For Docker: use container name 'keycloak'
# For local development outside Docker: use localhost:8080
KEYCLOAK_URL=http://keycloak:8080
KEYCLOAK_REALM=ecssr-events
KEYCLOAK_CLIENT_ID=ecssr-events-app
KEYCLOAK_CLIENT_SECRET=zUGl2wSob41YEpHuyO2vvPV5TXPdHmCw
KEYCLOAK_REDIRECT_URI=http://localhost:5050/api/auth/callback
KEYCLOAK_ADMIN=admin
KEYCLOAK_ADMIN_PASSWORD=admin

# Legacy admin credentials (for bootstrap compatibility)
ADMIN_USERNAME=admin
ADMIN_PASSWORD=admin123

# Superadmin bootstrap (Keycloak user)
SUPERADMIN_EMAIL=admin@localhost.ae
SUPERADMIN_PASSWORD=admin123

# Email Configuration (Resend or SMTP)
RESEND_API_KEY=
FROM_EMAIL=noreply@localhost
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=
SMTP_FROM=

# WhatsApp Service
WHATSAPP_ENABLED=false
WHATSAPP_SERVICE_URL=http://whatsapp-service:3001
WHATSAPP_AUTH_PHRASE=dev-change-me
WHATSAPP_DEFAULT_GROUP=ECSSR Events

# Scraper Service
SCRAPER_SERVICE_URL=http://scraper-service:3002
SCRAPER_SERVICE_TOKEN=dev-change-this-scraper-token
SCRAPER_SERVICE_SECRET=dev-change-this-scraper-secret

# Scraper Flags
ABU_DHABI_SCRAPER_ENABLED=false

# MinIO Configuration (Archive Media Storage)
# For Docker: use container name 'minio'
MINIO_ENDPOINT=minio
MINIO_PORT=9000
MINIO_ACCESS_KEY=minioadmin
MINIO_SECRET_KEY=minioadmin
MINIO_BUCKET=ecssr-archive
MINIO_EVENTS_BUCKET=ecssr-events
MINIO_USE_SSL=false

# Frontend
VITE_API_BASE_URL=http://localhost:5050

# Application
PORT=5050
NODE_ENV=development

# AI Image Generation (optional - for sample data)
# Set to 'openai' for AI-generated images, or 'placeholder' for simple colored placeholders
AI_IMAGE_PROVIDER=placeholder
#AI_IMAGE_API_KEY=

# Elasticsearch Configuration
ELASTICSEARCH_URL=http://elasticsearch:9200
ELASTICSEARCH_USERNAME=elastic
ELASTICSEARCH_PASSWORD=eventcal-dev-password-2024
ELASTICSEARCH_ENABLED=true

# AI Chat Configuration
AI_CHAT_PROVIDER=openai
AI_CHAT_MODEL=gpt-4o-mini
OPENAI_API_KEY=